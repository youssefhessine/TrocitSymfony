<form method="post" action="{{ path('app_communaute_cont_delete', {'id': communaute.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ communaute.id) }}">
    <button class="btn">Delete</button>
</form>
