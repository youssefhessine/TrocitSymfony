{{ form_start(form) }}
    {{ form_widget(form) }}
    <br>
    <button style="background-color: #4CAF50; color: white; padding: 10px 20px; border: none; cursor: pointer;"  class="btn btn-save ">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
