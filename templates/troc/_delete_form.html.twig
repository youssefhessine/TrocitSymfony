<form method="post" action="{{ path('app_troc_delete', {'idTroc': troc.idTroc}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ troc.idTroc) }}">
    <button style="background-color: #f44336; color: white; padding: 10px 20px; border: none; cursor: pointer;">Delete</button>
</form>
