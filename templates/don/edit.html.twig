{% extends 'front.html.twig' %}

{% block title %}Edit Don{% endblock %}

{% block body %}
    <h1>Edit Don</h1>
<section class="py-5">
    <div class="container px-4 px-lg-5 mt-5">
        <div class="row gx-4 gx-lg-5 row-cols-2 row-cols-md-3 row-cols-xl-4 justify-content-center">

    {{ include('don/form.html.twig', {'button_label': 'Update'}) }}
        </div>
    </div>
</section>
    <a class="btn btn-outline-dark mt-auto" href="{{ path('don_index') }}">back to list</a>

    {{ include('don/delete.html.twig') }}
{% endblock %}
