<form method="post" action="{{ path('expertise_delete', {'id': expertise.id_expertise}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ expertise.id_expertise) }}">
    <button class="btn">Delete</button>
</form>
