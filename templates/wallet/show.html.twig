{% extends 'base.html.twig' %}

{% block title %}Wallet{% endblock %}

{% block body %}
    <h1>Wallet</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdWallet</th>
                <td>{{ wallet.idWallet }}</td>
            </tr>
            <tr>
                <th>NbJeton</th>
                <td>{{ wallet.nbJeton }}</td>
            </tr>
            <tr>
                <th>NbTransaction</th>
                <td>{{ wallet.nbTransaction }}</td>
            </tr>
            <tr>
                <th>DateTransaction</th>
                <td>{{ wallet.dateTransaction }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_wallet_index') }}">back to list</a>

    <a href="{{ path('app_wallet_edit', {'idWallet': wallet.idWallet}) }}">edit</a>

    {{ include('wallet/_delete_form.html.twig') }}
{% endblock %}
