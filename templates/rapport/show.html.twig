{% extends 'back.html.twig' %}

{% block title %}Rapport{% endblock %}

{% block body %}
    <h1>Rapport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Reference</th>
                <td>{{ rapport.reference }}</td>
            </tr>
            <tr>
                <th>TitreRapport</th>
                <td>{{ rapport.titreRapport }}</td>
            </tr>
            <tr>
                <th>DescriptionProduit</th>
                <td>{{ rapport.descriptionProduit }}</td>
            </tr>
            <tr>
                <th>DateRapport</th>
                <td>{{ rapport.dateRapport ? rapport.dateRapport|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ rapport.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rapport_index') }}">back to list</a>

    <a href="{{ path('rapport_edit', {'reference': rapport.reference}) }}">edit</a>

    {{ include('rapport/delete.html.twig') }}
{% endblock %}
