<form method="post" action="{{ path('rapport_delete', {'reference': rapport.reference}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport.reference) }}">
    <button class="btn">Delete</button>
</form>
